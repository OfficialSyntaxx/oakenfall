{
  "name": "oakenfall",
  "version": "1.15.0",
  "private": true,
  "description": "Oakenfall — living-world villager simulation and site build.",
  "scripts": {
    "test": "cd game && node soak-test.js",
    "build": "vite build && node site/build.js",
    "assets:extract": "node tools/extract-assets.mjs",
    "assets:rewrite": "node tools/rewrite-assets.mjs",
    "smoke": "npm run build && node tools/smoke-test.mjs",
    "build:site": "node site/build.js",
    "typecheck": "tsc --noEmit",
    "gameplay": "npm run build && node tools/gameplay-test.mjs",
    "verify": "npm run build && node tools/smoke-test.mjs && node tools/gameplay-test.mjs && node tools/scenario-test.mjs && node tools/editor-test.mjs && node tools/slots-test.mjs && node tools/steward-test.mjs",
    "editor": "npm run build && node tools/editor-test.mjs && node tools/slots-test.mjs && node tools/steward-test.mjs",
    "scenario": "npm run build && node tools/scenario-test.mjs",
    "cap:sync": "npm run build && cap sync",
    "android": "npm run cap:sync && cap open android",
    "ios": "npm run cap:sync && cap open ios"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3",
    "playwright": "^1.62.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
